const AA = "AA"

let initialState = {
        availableCategory: ["", "idea", "Quote", "Random", "task"],
}

export let settingsReducer = (state = initialState, action) => {
    switch (action.type) {
        default :
            return state
    }
}


export const aa = () => ({type: AA})





